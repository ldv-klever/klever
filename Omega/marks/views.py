from django.shortcuts import render


def create_mark(request, report_id):
    # TODO:
    # Get table of attributes, list of available functions, verdicts, statuses,
    # report data (format, job class, author or report,
    return render('marks/EditMark.html', {})
