# All command line argument will be printed at separate line and they aren't empty strings. So one empty string can
# safely separate different build commands from each other.
BUILD_CMDS_SEPARATOR = '\n'
